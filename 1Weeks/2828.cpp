#include<bits/std.c++.h>

using namespace std;

int n,m,j;
vector<int>arr
vector<int>v;

int main()
{
    cin>>n>>m;
    cin>>j;
    v.resize(m,0);
    for(int i = 0; i < j; i++)
    {
        int a{};
        cin>>a;
        arr.push_back(a);
    }

    for(int i = 0; i < n; i++)
    {
        
    }

}