#include <iostream>
#include <vector>

using namespace std;

int n, ret;
vector<int> v;

void Solution(int index1, int index2)
{
	if (index2 < n)
		return;

	int size = index2 - index1;

	for (int i = ind)
}

int main()
{
	cin >> n;
	v.resize(n,0);
	for (int i = 0; i < n; i++)
		cin >> v[i];

	for (int i = 0; i < n-1; i++)
	{
		
	}
}